# Frontend build aimed at the PRODUCTION API (branch `main`).
# Vercel production usually leaves VITE_API_BASE_URL empty (same-origin /api
# rewrite). Required for Capacitor / mobile production builds.

VITE_API_BASE_URL=https://reservly-api.onrender.com
VITE_GOOGLE_CLIENT_ID=
