# Frontend build aimed at the DEMO API (branch `demo`).
# For Vercel demo project with /api rewrite to the demo Render host, leave
# VITE_API_BASE_URL empty. Required for Capacitor / mobile demo builds.

VITE_API_BASE_URL=https://reservly-api-demo.onrender.com
VITE_GOOGLE_CLIENT_ID=
